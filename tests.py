import pandas as pd
import sqlite3

# Connect to the SQLite database
# conn = sqlite3.connect('bookings.db')

def print_table(table_name="tickets"):
    # df = pd.read_sql_query(f"SELECT * FROM {table_name}", conn)
    print("00df")


# Close the connection
# conn.close()